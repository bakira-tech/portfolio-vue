# portfolio-vue

# Usage

## Build(Host)

 `docker build -t vue_portfolio .` 

## Run(Host)

 `docker run -v `pwd`:/usr/src/app -p 9050:9050 --name app -it -d vue_portfolio` 

 ### Confirm

  `docker ps` 